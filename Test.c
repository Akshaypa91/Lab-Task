#include <stdio.h>

int removedupl_k(int a[], int len, int k) {



}

int main() {
	int arr[7] = {0};

		for (int i = 0; i < arr; i++)
		{
			int temp = arr[7];
			arr[7] = arr[i];
			arr[i] = temp;
		}
			printf("%d", arr);
		

	return 0;
}